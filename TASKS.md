# UI/UX Redesign Tasks

## Выполнено
- [x] Очистить глобальные стили от legacy CSS и оставить только дизайн-токены/базу Tailwind.
- [x] Убрать "синий" оттенок темы, перейти на более нейтральную тёмную базу с акцентным primary.
- [x] Сделать тёмную тему дефолтной.
- [x] Исправить layout-скролл: sidebar закреплен (`sticky`), контент скроллится единым потоком страницы.
- [x] Выравнять структуру проблемных страниц к единому порядку: заголовок → фильтры → инфо-блок → контент.
- [x] Добавить `ui/Input.vue` в стиле shadcn.
- [x] Перевести ключевые кнопки/инпуты на `ui`-слой (`Button`, `Input`, `Badge`) в основных вьюхах.

## Следующая итерация (рекомендуется)
- [ ] Довести до 100% миграцию всех legacy-компонентов (`RevenueFilters`, `StopListFilters`) либо удалить неиспользуемые.
- [ ] Добавить единый `ReportPageHeader` компонент для всех отчетных страниц.
- [ ] Пройтись по таблицам и ввести единый `DataTable`-шаблон (sticky head, mobile cards, states).
