# План разработки дашборда Panda

---

## Текущее состояние проекта

| Модуль         | Готовность |
| -------------- | ---------- |
| Telegram-бот   | ~80%       |
| Backend (API)  | ~60%       |
| Web-panel (UI) | ~40%       |

---

## Дорожная карта

### Этап 1 — Фундамент UI (приоритет: критический)

**Проблема:** Web-panel построена без shadcn-vue и Tailwind CSS — нужен рефакторинг технологического стека, прежде чем двигаться дальше.

- [x] Установить и настроить **Tailwind CSS v3** + shadcn-vue в web-panel
- [x] Настроить **темизацию** (CSS-переменные, `darkMode: 'class'`, `useThemeStore`)
- [x] Реализовать **ThemeToggle** компонент (переключение тёмной/светлой темы)
- [x] Создать базовый компонент **MetricCard** (значение + LFL + прогресс-бар плана)
- [x] Создать компонент **LFLBadge** (зелёный/красный/нейтральный, поддержка `inverse`)
- [x] Обновить **AppHeader** (Лого + PeriodSelector + DepartmentSelector + ThemeToggle)

---

### Этап 2 — Фильтры (приоритет: высокий)

**Проблема:** `PeriodSelector` без пресетов, `DepartmentSelector` отсутствует, LFL-диапазоны не вычисляются на фронте.

- [x] Реализовать **PeriodSelector** с пресетами: Сегодня / Вчера / Текущая неделя / Прошлая неделя / Текущий месяц / Прошлый месяц / Текущий год / Кастомный
- [x] Реализовать **DepartmentSelector** — мультиселект с иерархией (Город → Рестораны)
- [x] Реализовать **useFiltersStore** (Pinia) — период, диапазон дат, подразделения
- [x] Реализовать composable **usePeriod** с вычислением LFL-диапазонов для каждого пресета

---

### Этап 3 — Маркетинговые метрики (приоритет: высокий)

**Backend:**

- [x] Добавить `POST /api/analytics/revenue` — выручка с LFL по каналам
- [x] Добавить `POST /api/analytics/hourly-sales` — продажи по часам
- [x] Добавить `POST /api/analytics/operational` — время доставки/приготовления

**Frontend:**

- [x] Карточки выручки по каналам с LFL
- [x] **AreaChart** — динамика выручки по дням
- [x] **DonutChart** — структура выручки по каналам
- [x] **HourlyBarChart** — продажи по часам (переключатель выручка/заказы)
- [x] Карточки дисконта (абсолютный + %) — поля RevenueWithoutDiscount + DiscountSum в OLAP

---

### Этап 4 — Операционные метрики (приоритет: средний)

**Backend:**

- [x] `POST /api/analytics/operational` — время доставки и приготовления
- [x] `POST /api/analytics/courier-routes` — маршруты курьеров (через Delivery.Courier.Id / Delivery.Courier)
- [x] Foodcost — реализован через OLAP-поля себестоимости

**Frontend:**

- [x] Операционные метрики на Дашборде (avgDeliveryTime, avgCookingTime, avgPerOrder, totalOrders)
- [x] **DonutChart** маршрутов курьеров (Delivery.Courier.Id)

---

### Этап 5 — Топ блюд (приоритет: средний)

**Backend:**

- [x] `POST /api/analytics/top-dishes` — топ-20 и аутсайдеры (группировка по Dish.Name + ProductCategory)

**Frontend:**

- [x] **TopDishesView** — топ-20 и аутсайдеры (сортировка, поиск, % от итога)
- [x] Переключение «Топ» / «Аутсайдеры»
- [x] Маршрут `/top-dishes`, навигация в Sidebar + Dashboard

---

### Этап 6 — Клиентская база (приоритет: средний)

- [x] Реализовать `GET /api/analytics/clients` — активная база + новые клиенты (PremiumBonus API)
- [x] Graceful fallback при ненастроенном API (отображение инструкции по настройке)
- [x] Карточки «Активная база» и «Новые клиенты»
- [x] Маршрут `/clients`, навигация в Sidebar + Dashboard

---

### Этап 7 — Управление планами (приоритет: низкий)

- [ ] Хранилище для планов: JSON-файл
- [ ] REST API `/api/plans` (GET / POST / PUT / DELETE)
- [ ] **PlansView.vue** — CRUD-интерфейс (период, метрика, подразделение, целевое значение)
- [ ] Интеграция прогресс-бара выполнения плана в MetricCard

---

### Этап 8 — Полировка (приоритет: низкий)

- [ ] Без авторизации
- [ ] Skeleton-загрузка во всех компонентах
- [ ] Toast-уведомления при ошибках (shadcn-vue `<Sonner>`)
- [ ] Авто-обновление данных каждые 5 минут
- [ ] Адаптивность (mobile breakpoints)
- [ ] Таблица разбивки по подразделениям (если выбрано несколько)
- [ ] Добавить логирование в middleware

---

## Открытые вопросы, требующие решения перед реализацией

| Вопрос                                                             | Кому              | Этап |
| ------------------------------------------------------------------ | ----------------- | ---- |
| Поле `ReadyTime` (простой на тепловом столе) в iiko OLAP           | Тестирование iiko | 4    |
| Поле `CourierRoute` или аналог в iiko OLAP                         | Тестирование iiko | 4    |
| PremiumBonus API: фильтрация по `lastOrderDate` / `firstOrderDate` | Маркетинг         | 6    |
| `CloseTime − OpenTime` — корректная метрика для времени зала?      | Тестирование iiko | 4    |

---

**Рекомендуемый старт:** Этап 1 (установка Tailwind + shadcn-vue + MetricCard + LFLBadge) — без него все последующие этапы будут строиться на устаревшей базе.
